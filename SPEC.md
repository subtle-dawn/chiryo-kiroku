# 治療記録 SPEC.md

## 1. 概要

### 1.1 アプリ名
**治療記録**

### 1.2 目的
治療中の病気・症状について、以下の情報を病気ごとに時系列で記録・閲覧できる個人向けWebアプリ。

- 症状の経過
- 通院記録
- 診察時に医師と話した内容
- 医師から言われたこと
- 処方された薬
- 薬の効果
- 薬の副作用
- 検査
- その他の治療上の出来事
- 次回診察で相談したいこと

「お薬手帳」や「体調日記」ではなく、**病気ごとの治療経過を一本のタイムラインとして振り返れること**を中心にする。

---

## 2. 基本方針

### 2.1 完全クライアントサイド
初期版ではサーバーを使用しない。

- ログインなし
- アカウントなし
- APIサーバーなし
- 外部DBなし
- 広告なし
- アクセス解析なし
- 個人の治療記録を運営側へ送信しない

データはユーザーのブラウザ内にのみ保存する。

### 2.2 ホスティング
GitHub Pagesで静的ホスティングする。

### 2.3 想定技術
- Vite
- React
- TypeScript
- IndexedDB
- Dexie.js
- React Router
- CSS
- PWA対応

大規模なUIライブラリは初期版では必須としない。

---

## 3. 対象端末

優先順位は以下。

1. スマートフォン
2. PC
3. タブレット

スマートフォンで診察前・診察後にすぐ記録できることを重視する。

レスポンシブ対応必須。

---

## 4. 中心となる概念

### 4.1 病気
記録をまとめる最上位単位。

例：

- 偏頭痛
- 花粉症
- 腰痛

病名はユーザーが自由入力する。

### 4.2 タイムライン
各病気について発生した出来事を日時順に並べる。

例：

```text
偏頭痛

次に相談すること
漢方を飲んだら悪化した

2026/08/21
通院
A病院
漢方処方

2026/08/22
症状
頭痛い

2026/08/22
通院
B病院
〇〇と言われた
```

異なる種類の記録も、病気単位では同じタイムラインに表示する。

---

## 5. 画面構成

初期版では以下の画面を実装する。

1. ホーム画面
2. 病気詳細画面
3. 病気追加・編集画面
4. 記録追加・編集画面
5. バックアップ・復元画面
6. 設定画面

---

# 6. ホーム画面

## 6.1 目的
登録されている病気を一覧表示し、現在の治療状況へ素早くアクセスできるようにする。

## 6.2 表示内容
病気ごとにカードを表示する。

カードには以下を表示する。

- 病名
- 次に相談すること
- 最終更新日

例：

```text
┌─────────────────────┐
│ 偏頭痛              │
│                     │
│ 次に相談すること     │
│ 漢方を飲んだら悪化した│
│                     │
│ 最終更新 2026/08/22 │
└─────────────────────┘
```

## 6.3 操作
病気カードを押すと病気詳細画面へ移動する。

画面上に「病気を追加」ボタンを置く。

## 6.4 空状態
病気がまだ登録されていない場合：

```text
まだ病気が登録されていません。

[病気を追加]
```

---

# 7. 病気詳細画面

## 7.1 ヘッダー
以下を表示する。

- 病名
- 編集ボタン
- 戻るボタン

## 7.2 次に相談すること
病気ごとに「次回診察で相談したいこと」を表示する。

例：

```text
次に相談すること

漢方を飲んだら悪化した

[編集]
```

複数行入力可能。

空の場合：

```text
次に相談することはありません

[追加]
```

## 7.3 タイムライン
その病気に紐づく記録を新しい順に表示する。

デフォルト：
**新しい → 古い**

各記録には以下を表示する。

- 日付
- 時刻（入力されている場合のみ）
- 記録種別
- タイトルまたは主要情報
- 本文

例：

```text
2026/08/22

[通院]
B病院

〇〇と言われた
```

```text
2026/08/22

[症状]

頭痛い
```

```text
2026/08/21

[通院]
A病院

漢方処方
```

## 7.4 操作
- 記録追加
- 記録編集
- 記録削除
- 次に相談すること編集
- 病気編集

---

# 8. 記録種別

初期版では以下を用意する。

```ts
type RecordType =
  | "visit"
  | "symptom"
  | "medication"
  | "effect"
  | "side_effect"
  | "test"
  | "memo";
```

日本語表示：

| 値 | 表示名 |
|---|---|
| visit | 通院 |
| symptom | 症状 |
| medication | 薬・処方 |
| effect | 薬の効果 |
| side_effect | 副作用 |
| test | 検査 |
| memo | メモ |

後から種類を追加できる設計にする。

---

# 9. 記録追加画面

## 9.1 共通項目

すべての記録で以下を持つ。

### 必須
- 病気
- 記録種別
- 日付
- 本文

### 任意
- 時刻
- タイトル
- 関連する病院
- 関連する薬
- メモ

## 9.2 記録種別ごとの補助項目

### 通院
入力候補：

- 病院名
- 診療科
- 医師名
- 診察で相談したこと
- 医師から言われたこと
- 処方・治療内容
- 次回受診予定
- 自由記述

すべて任意とする。

「通院」という記録を気軽に残せることを優先し、必須項目を増やしすぎない。

### 症状
入力候補：

- 症状
- 程度
- 発生時刻
- 持続時間
- 自由記述

程度は任意。

例：

```text
軽い
普通
強い
かなり強い
```

### 薬・処方
入力候補：

- 薬名
- 用量
- 回数
- 使用方法
- 開始日
- 終了日
- 処方理由
- 自由記述

### 薬の効果
入力候補：

- 薬名
- 効果
- 効果の程度
- 自由記述

### 副作用
入力候補：

- 薬名
- 症状
- 程度
- 自由記述

### 検査
入力候補：

- 検査名
- 病院名
- 結果
- 自由記述

### メモ
自由記述中心。

---

# 10. 入力UX

## 10.1 少ない操作で保存できること
詳細入力を強制しない。

最低限、

```text
種別
日付
本文
```

だけで保存可能とする。

## 10.2 日付
新規記録作成時は当日の日付を初期値とする。

## 10.3 時刻
時刻は任意。

## 10.4 保存後
保存したら病気詳細画面へ戻り、追加した記録がタイムラインに表示される。

---

# 11. 病気データ

データ構造例：

```ts
interface Condition {
  id: string;
  name: string;
  nextConsultationNote: string;
  createdAt: string;
  updatedAt: string;
}
```

### id
UUIDを使用する。

---

# 12. 記録データ

初期版では柔軟性を優先し、共通項目＋追加情報で持つ。

```ts
interface TreatmentRecord {
  id: string;
  conditionId: string;

  type: RecordType;

  date: string;
  time?: string;

  title?: string;
  body: string;

  hospitalName?: string;
  departmentName?: string;
  doctorName?: string;

  medicationName?: string;

  details?: Record<string, string | number | boolean | null>;

  createdAt: string;
  updatedAt: string;
}
```

---

# 13. IndexedDB

Dexie.jsを使用する。

例：

```ts
class TreatmentDatabase extends Dexie {
  conditions!: Table<Condition>;
  records!: Table<TreatmentRecord>;

  constructor() {
    super("treatment-record");

    this.version(1).stores({
      conditions: "id, name, updatedAt",
      records: "id, conditionId, type, date, createdAt"
    });
  }
}
```

DB名：

```text
treatment-record
```

---

# 14. データの並び順

## 病気一覧
デフォルトでは最終更新日時の新しい順。

## タイムライン
以下の順で降順。

1. date
2. time
3. createdAt

同じ日・時刻不明の場合は新しく追加したものを上に表示する。

---

# 15. 編集

病気と記録は後から編集可能とする。

編集画面では既存値を初期表示する。

保存すると `updatedAt` を更新する。

---

# 16. 削除

病気と記録は削除可能。

## 16.1 記録削除
確認ダイアログを出す。

```text
この記録を削除しますか？

[キャンセル]
[削除]
```

## 16.2 病気削除
病気を削除すると、その病気の記録もすべて削除される。

必ず確認する。

例：

```text
「偏頭痛」を削除します。

この病気に登録されているすべての記録も削除されます。
この操作は元に戻せません。

[キャンセル]
[削除]
```

---

# 17. バックアップ

ブラウザ内データは消失する可能性があるため、初期版から必須機能とする。

## 17.1 JSONエクスポート
すべてのデータをJSONとしてダウンロードできる。

ファイル名：

```text
治療記録_backup_YYYY-MM-DD.json
```

例：

```json
{
  "version": 1,
  "exportedAt": "2026-08-22T12:00:00.000Z",
  "conditions": [],
  "records": []
}
```

## 17.2 JSONインポート
エクスポートしたJSONから復元可能。

### 復元方式
初期版では以下の2つから選ばせる。

- 現在のデータを置き換える
- 現在のデータへ追加する

置き換えは必ず確認ダイアログを表示する。

## 17.3 不正ファイル
形式が異なる場合は復元せず、エラー表示する。

---

# 18. PWA

インストール可能なPWAにする。

必要：

- manifest
- service worker
- app icons
- standalone表示
- theme color

PWA化には `vite-plugin-pwa` の利用可。

## 18.1 オフライン
一度読み込んだ後は、原則オフラインでも以下を使用可能にする。

- 病気一覧表示
- 病気詳細表示
- 記録追加
- 記録編集
- 記録削除
- バックアップ書き出し

---

# 19. GitHub Pages

GitHub Pagesで正常動作するようにする。

Viteの `base` 設定に注意する。

独自ドメインを設定しない場合でも、

```text
https://USERNAME.github.io/REPOSITORY/
```

で動作すること。

React Routerを使う場合、GitHub Pages上で404問題が起きない方法を採用する。

初期版では `HashRouter` を使用してもよい。

例：

```text
/#/
/#/condition/xxxx
/#/record/new
```

シンプルさを優先する。

---

# 20. デザイン方針

## 20.1 方針
医療アプリ風にしすぎず、個人用の記録帳として落ち着いたUIにする。

- シンプル
- 読みやすい
- 情報密度を高くしすぎない
- スマホで押しやすい
- 記録内容を最優先

## 20.2 タイムライン
記録種別が一目で分かるよう、種別ラベルを表示する。

例：

```text
[通院]
[症状]
[薬・処方]
[効果]
[副作用]
[検査]
[メモ]
```

色だけに依存せず、必ず文字でも表示する。

---

# 21. アクセシビリティ

最低限以下を守る。

- ボタンに分かりやすいラベル
- 十分なタップ領域
- キーボード操作可能
- 色だけで意味を区別しない
- inputにはlabelを付ける
- 適切なHTML要素を使う
- 本文の文字サイズを小さくしすぎない

---

# 22. プライバシー

このアプリでは極めてセンシティブな治療情報を扱う。

初期版では以下を徹底する。

- サーバーへ治療情報を送信しない
- Google Analytics等を導入しない
- 広告SDKを導入しない
- 外部ログサービスへ入力内容を送信しない
- エラー報告サービスへ治療記録本文を送信しない
- URLへ病名や記録本文を含めない

READMEにも以下の趣旨を明記する。

```text
記録したデータは、この端末のブラウザ内に保存されます。
運営者のサーバーへ治療記録が送信されることはありません。
```

---

# 23. セキュリティ上の注意

完全クライアントサイドのため、アプリ独自のアカウント認証は実装しない。

ただし、

- 同じ端末・ブラウザを利用できる人には記録を見られる可能性がある
- ブラウザデータ削除で記録が消える可能性がある

ことを設定画面等で説明する。

初期版ではPINロックは実装しない。

将来機能候補とする。

---

# 24. 設定画面

初期版では以下を置く。

- バックアップを書き出す
- バックアップから復元
- このアプリについて
- データ保存について
- 全データ削除

## 全データ削除
確認を2段階程度入れてもよい。

---

# 25. 初回起動

初回起動時に短い説明を表示する。

例：

```text
治療記録

病気ごとに、症状・通院・処方・薬の効果や副作用を
時系列で記録できます。

記録はこのブラウザ内に保存され、
サーバーには送信されません。

[はじめる]
```

長いチュートリアルは不要。

---

# 26. 日付・時刻

表示は日本語環境を前提として、

```text
2026/08/22
```

形式を基本とする。

内部保存はISO形式を推奨。

例：

```text
2026-08-22
22:30
2026-08-22T13:30:00.000Z
```

---

# 27. 病院名・薬名

初期版ではマスターデータを持たない。

自由入力とする。

入力履歴から候補表示できる設計は将来追加可能。

---

# 28. 検索・絞り込み

MVPでは必須ではない。

ただしデータ構造は、将来以下が追加可能な形にする。

- 全文検索
- 記録種別で絞り込み
- 日付範囲
- 病院
- 薬
- 症状

---

# 29. 「次に相談すること」

この機能は重要度が高い。

病気単位で保持する。

MVPでは単一のテキスト欄でよい。

```ts
nextConsultationNote: string
```

将来的には複数項目のチェックリスト形式に拡張できる。

例：

```text
・漢方を飲んだら悪化した
・この頭痛は偏頭痛なのか聞く
・薬を続けてよいか
```

初期版では改行可能なtextareaとして実装する。

---

# 30. 更新日時

病気カードの「最終更新」は、

- 病気そのものを編集した日時
- 次に相談することを編集した日時
- その病気に記録を追加・編集した日時

のうち最新のものを使用する。

---

# 31. MVPで実装しないもの

初期版では以下は実装しない。

- ユーザー登録
- ログイン
- クラウド同期
- 他端末同期
- 医師との共有
- 家族との共有
- SNS機能
- 通知
- プッシュ通知
- 広告
- 課金
- AI診断
- AIによる医療助言
- 医療機関API連携
- マイナポータル連携
- 電子お薬手帳連携
- 自動バックアップ
- 写真添付
- PDF出力
- CSV出力
- PINロック
- 生体認証

---

# 32. 将来機能候補

以下はMVP完成後に検討する。

## 診察
- 診察モード
- 「次に相談すること」だけを大きく表示
- 診察中にチェック
- 診察終了後に通院記録へ変換

## 薬
- 薬ごとのページ
- 処方開始・中止履歴
- 効果と副作用を薬単位で一覧
- 服薬中の薬一覧

## 病院
- 病院ごとの履歴
- 診療科
- 医師

## 出力
- 診察用サマリー
- PDF
- 印刷
- CSV

## データ保護
- PINロック
- Web Crypto APIを利用したローカル暗号化

## 入力
- テンプレート
- よく使う記録
- 音声入力

## 可視化
- 症状の強さグラフ
- 薬変更前後の比較

---

# 33. ディレクトリ構成案

```text
src/
├─ app/
│  ├─ App.tsx
│  └─ router.tsx
│
├─ db/
│  ├─ db.ts
│  ├─ conditions.ts
│  └─ records.ts
│
├─ types/
│  ├─ condition.ts
│  └─ treatmentRecord.ts
│
├─ pages/
│  ├─ HomePage.tsx
│  ├─ ConditionPage.tsx
│  ├─ ConditionEditPage.tsx
│  ├─ RecordEditPage.tsx
│  ├─ BackupPage.tsx
│  └─ SettingsPage.tsx
│
├─ components/
│  ├─ ConditionCard.tsx
│  ├─ Timeline.tsx
│  ├─ TimelineItem.tsx
│  ├─ RecordTypeBadge.tsx
│  ├─ ConsultationNote.tsx
│  ├─ Header.tsx
│  └─ ConfirmDialog.tsx
│
├─ services/
│  ├─ backup.ts
│  └─ restore.ts
│
├─ utils/
│  ├─ date.ts
│  └─ uuid.ts
│
├─ styles/
│  └─ global.css
│
└─ main.tsx
```

厳密にこの構成へ従う必要はないが、責務を分離する。

---

# 34. ルーティング案

HashRouterの場合：

```text
/#/
/#/condition/new
/#/condition/:conditionId
/#/condition/:conditionId/edit
/#/condition/:conditionId/record/new
/#/condition/:conditionId/record/:recordId/edit
/#/backup
/#/settings
```

---

# 35. 完了条件

MVPは以下をすべて満たしたら完成とする。

- GitHub Pagesで公開できる
- スマートフォンで正常に操作できる
- 病気を追加できる
- 病気を編集できる
- 病気を削除できる
- ホームに病気一覧が表示される
- 「次に相談すること」を保存できる
- 各病気に記録を追加できる
- 異なる種類の記録が同一タイムラインに表示される
- 記録を編集できる
- 記録を削除できる
- IndexedDBへ永続保存される
- ページ再読み込み後も記録が残る
- JSONバックアップを書き出せる
- JSONから復元できる
- 外部サーバーへ治療記録を送らない
- オフラインで基本操作ができる
- PWAとしてホーム画面へ追加可能
- TypeScriptのビルドエラーがない
- READMEに起動方法・ビルド方法・GitHub Pages公開方法を書く

---

# 36. Codexへの実装指示

このSPECに基づき、まずMVPを実装すること。

優先順位：

1. データモデル
2. IndexedDB
3. ホーム画面
4. 病気追加・編集
5. 病気詳細タイムライン
6. 記録追加・編集
7. 削除
8. 次に相談すること
9. バックアップ・復元
10. PWA
11. GitHub Pages対応
12. UI調整

過剰な抽象化は避ける。

MVPに不要なバックエンド、認証、クラウドサービス、分析SDK等を追加しない。

医療情報を扱うが、このアプリは診断・治療判断を行わない。
単なる個人用の記録・閲覧ツールとして実装する。

---

# 37. サンプルデータ

開発時の確認用として以下を利用してよい。

```text
病気
偏頭痛

次に相談すること
漢方を飲んだら悪化した
```

タイムライン：

```text
2026/08/21
通院
A病院
漢方処方

2026/08/22
症状
頭痛い

2026/08/22
通院
B病院
〇〇と言われた
```

本番ビルドにはサンプルデータを自動登録しない。
